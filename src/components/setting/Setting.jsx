import React from 'react';

const Setting = ({ match }) => {
  return(<div>
    jkhgf
    {/*<h3>{match.params.setting}</h3>
    <p>
      {match.params.setting} settings
    </p>*/}
  </div>);
};

export default Setting;